<h2 class="">Countries of the NBA Players</h2>

<ul>
  <% @countries.each do |country| %>
    <li>
      <%= link_to "#{country.name}", country %>
    </li>
  <% end %>
</ul>

<%= paginate @countries %>